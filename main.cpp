#include <iostream>
#include "fraction_calculation.h"

using namespace std;

int main() {
    /*fraction_calculation frac1 (27, 3);
    frac1.printFraction();
    fraction_calculation frac2 (3, -2);
    frac2.printFraction();
    fraction_calculation summa = fraction_calculation::addition (frac1, frac2);
    fraction_calculation mult = fraction_calculation::multiplication(frac1, frac2);
    fraction_calculation div = fraction_calculation::division(frac1, frac2);
    summa.printFraction();
    mult.printFraction();
    div.printFraction();*/
    return 0;
}
